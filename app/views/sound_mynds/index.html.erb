<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sound Mynd</title>
</head>
<body>
    <div class="header">
        <h1>Welcome, <%= current_user.nickname %>!</h1>
        <a href="/users/<%= current_user.id %>"></a>
    </div>
    <div class="group_container">
        <div class="group_sessions">
            <h1>Group Sessions</h1>
            <% @groups.each do |group| %>
                <div class="sess">
                    <h3><%= group.name %></h3>
                    <p><%= group.desc %></p>
                    <p><%= group.time.strftime("%l:%M %p") %></p>
                    <p><%= group.location %></p>
                    <% if current_user.groups_attending.include? group %>
                        <p>Attending
                        <a href="attends/<%= group.id %>" data-method="delete" >Cancel</a></p>
                    <% else %>
                        <a href="attends/<%= group.id %>" data-method="patch">Attend</a>
                    <% end %>
                </div>
                <br>
            <% end %>
        </div>
        <div class="chem_depend">
            <h1>Chemical Dependency Sessions</h1>
            <% @groups1.each do |group| %>
                <div class="sess">
                    <h3><%= group.name %></h3>
                    <p><%= group.desc %></p>
                    <p><%= group.time.strftime("%l:%M %p") %></p>
                    <p><%= group.location %></p>
                    <% if current_user.groups_attending.include? group %>
                        <p>Attending
                        <a href="attends/<%= group.id %>" data-method="delete">Cancel</a></p>
                    <% else %>
                        <a href="attends/<%= group.id %>" data-method="patch">Attend</a>
                    <% end %>
                </div>
                <br>
            <% end %>
        </div>
    </div>
    <div class="counselor_container">
        <div class="counselors">
            <h1>Counselors Onsite</h1>
            <h3>Open Door Policy. Walk-ins Always Welcome.</h3>
            <% @counselors.each do |counselor| %>
                <div class="sess">
                    <p><a href="/counselors/<%= counselor.id %>"><%= counselor.first_name %></a></p>
                    <p>Specialty: <%= counselor.specialty %></p>
                </div>
                <br>
            <% end %>
        </div>
    </div>
</body>
</html>